[tool.ruff]
line-length = 110

[tool.ruff.lint]
ignore = [
    "B008",    # FastAPI's Depends() pattern requires calls in argument defaults
    "BLE001",  # intentional broad excepts for graceful ML/RAG fallback behavior
]
